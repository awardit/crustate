/* @flow */

import { useContext } from "preact/hooks";
import { createContext
       , h as createElement
       , Component } from "preact";

export default {
  useContext,
  createContext,
  createElement,
  Component,
}